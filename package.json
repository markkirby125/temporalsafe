{
  "name": "temporalsafe",
  "version": "0.1.0",
  "description": "Page-level flicker and flash reducer for photosensitive and migraine users — userscript and demo page.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node build.js"
  }
}
